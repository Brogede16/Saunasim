Original prompt: Continue the Sauna Sim project iteratively, keeping the approved exterior-only, mobile pixel-art design and production planning coherent.

## 2026-08-24

- Added the first visual production manifest and fixed Canal Workshop production spec.
- Added an initial React + Phaser vertical-slice scaffold for Canal + Repair Workshop.
- The world is renderer-only placeholder pixel geometry. The simulation state stays outside Phaser in `src/sim/game.ts`.
- Current interaction: rename the venue and buy six independent modules. Canvas reacts to purchases.
- Installed dependencies and completed browser checks for the arrival-sign and program-sauna purchases. Both updated cash and visible canvas modules without browser errors.
- Added IndexedDB autosave with schema validation. A browser reload test confirmed that a purchased shower persisted with the same cash and visible scene state.
- Re-ran the standard browser smoke test after autosave; the cold-plunge module still updated independently without errors.
- Added a deterministic first guest layer: each completed week now creates 4-8 named, inspectable English-language guest profiles with independent age, visit goal, likes/dislikes, activity and grounded feedback. It flags the specific cold-recovery bottleneck when the plunge lacks a shower, and makes high entry prices visible as aborted visits. The selected guest is saved locally and can be selected in both the dock and the canvas.
- Verified the guest slice with 12 unit tests, a production build, and browser checks for selecting a guest from both the list and the rendered canvas. The local dev server is running on `http://127.0.0.1:5175/` for continued testing.
- Guest samples now move on short, activity-directed prototype routes and remain directly clickable throughout the motion. These temporary coordinates will be replaced by the authored route anchors from each finished asset card.
- Began the persistent Aufguss Builder: active program name, intent, heat, format, performance and music are now game state. Default values preserve the current balance; Intent already changes truthful guest feedback. Next builder layer: aroma rounds, delivery forms, recovery finishes, derived timing and first-run tier reveal.
- Added the first physical recovery-finish bridge: Outdoor Shower and Cold Plunge can be selected only when visibly owned, and a subset of participating guests now walks to the selected finish with matching feedback.
- Added the first real aroma round to the saved program builder: all 30 approved materials and six delivery forms are selectable, classified for later per-session cost balancing, and the chosen material now appears in Gus feedback. Next: multiple ordered rounds, material cost and composition-tier evaluation.
- Expanded the builder to one through three ordered aroma rounds with independent delivery forms. The selected order is now stable save data and ready for timing, cost and composition evaluation.
- Added a pre-session program preview: derived room time, guest-journey time, per-session material input estimate and a context-sensitive flow note. Composition remains correctly `Unproven` until the first actual run.
- Closed the first composition loop: a Gus is `Unproven` until a Master runs it; then it reveals Bad, Normal, Rare or Iconic. Any edit resets the reveal. The current authored rules are intentionally small and will expand only with approved combination content.
- Added `docs/first-slice-balance-audit-v0.1.md`: P0 connections are real program economics/capacity, separate Execution and Venue Fit, and live routed visits. Numerical balance must wait until those are connected.
- Corrected first active-program capacity: a single Master now uses one visible room/field at a time, while the facility determines session size. The future fairness rule is margin/return per Master-minute, not simplistic revenue per seat.
- Added `Bench Refit` as the controlled first invisible upgrade: +3 capacity in the current physical sauna, without creating a room/lane. It is documented as the only invisible exception and will later trade against intimacy at high occupancy.
- Split Gus capacity from actual attendance and occupancy. The weekly report now exposes `guests / capacity` and fill percentage; active-program demand responds to intent and supplement price before capacity can become revenue.
- Added `docs/canonical-vocabulary-v0.1.md` and elevated it in the docs reading order. It confirms shared core terms and explicitly lists staged gaps so the code's smaller current lists cannot overwrite the approved design canon.
- Added `docs/game-systems-map-v0.1.md`, the single integrated explanation of the game's major types and their relationships. It links each subsystem to its detailed canonical source.
- Added `docs/remaining-work-overview-v0.1.md`, a prioritised P0-P3 overview distinguishing first-playable work from later expansion and release work.
- Added workspace structure: `assets/`, `src/content/`, `tests/scenarios/` and `output/exports/`, with documentation describing ownership and intended production flow.
- Added first separate Program Review layers: Composition, Execution, Venue Fit and whole-star result. Current Canal rules are deliberately conservative scaffolding for later Master, location and asset data.
- Replaced the boolean-only Master state with a Starter Master profile. Heat, Aroma and Performance Craft now affect demanding heat, multi-round aroma and choreography; style can lift matching Rare-program execution.
- Added first automated Canal capacity scenarios: compact baseline, Bench Refit creates supply but not demand, and social appeal is checked against higher Gus-price resistance.
- Replaced guest activity-string routing with saved, typed visit paths and current stops. The scene and inspection card now use the same canonical route data for arrival, program, recovery, shop, queue and exit.
- Browser-verified the canonical guest-route slice at `http://127.0.0.1:5175/`: the selected card and scene report the same `arrival → program → exit` visit data, with no console errors.
- Added venue opening schedule data: days and hours now affect potential demand, operational costs and the feasible requested Gus session count. The first schedule scenario is covered by automated tests.
- Added first maintenance slice: only Program Sauna, shower and cold plunge deteriorate; a hired technician takes one timed repair task and restores only its selected module.
- Normalised first-room Gus capacity to eight visible places, with Bench Refit at eleven, Outdoor Gus Yard at twenty and Program Sauna Volume at twenty-four. This now matches the production/balance reference rather than using temporary seven-seat placeholders.
- Added six automated Canal operating scenarios covering a healthy starter week, unsupported entry pricing, capacity without demand, social-program demand, truthful recovery requirements and loan pressure.
- Bumped local autosave to schema v6 after schedule, program and maintenance state became mandatory; incompatible older prototype saves now cleanly start a fresh test game instead of partially hydrating.
- Added local save export/import. Autosave remains automatic in IndexedDB; an exported JSON backup validates its complete schema before it may overwrite the local test game. This keeps the no-login prototype usable over multiple days and portable without pretending it is cloud sync.
- Corrected cold-recovery routing: a cold-plunge queue is now created only by actual cold-finish demand, and a built shower visibly becomes a rinse-then-plunge route for a subset of participating guests while also relieving the simulated throughput pressure.
- Completed the code vocabulary for all ten music directions and six recovery finishes, including disabled Canal-only choices where no physical facility exists yet. The six approved Ultimate patterns are now exact deterministic composition discoveries, covered by tests; they still grant no automatic profit or sixth star. Autosave schema is v7 because old `Silence` saves cannot truthfully map to the approved music vocabulary.
- Added the first runtime scene contract at `src/content/canalWorkshopScene.ts`. It encodes Canal's 960x640 world, six current module fields, activity/effect anchors, camera limits and route graph. It is tested for field bounds and one-to-one visible module routing; the current placeholder renderer is the next code consumer.
- Connected the current renderer to scene anchors and removed its guest yoyo loop. Visible sampled guests now walk once to the current activity named on their profile and remain there, while exit guests use staggered departure positions. This is the first honest route presentation; timed multi-stage visits remain the next layer.
- Corrected exterior-only indoor Gus rendering after visual QA: indoor program participants now gather at the workshop entry transition instead of appearing on the building facade. Outdoor Gus retains its own visible yard anchors.
- Generated and stored the first Canal environment-base composition draft at `assets/source/backgrounds/canal-01-base-day-draft-v01.png`. It is explicitly source-only: useful for parcel/promenade/canal composition and palette review, but too detailed and not aligned enough to promote into the runtime asset set without a cleanup pass.
- Generated a Repair Workshop building reference draft, then rejected it for runtime use because the apparent checkerboard was baked into an RGB image rather than exported as true alpha. The rejection and retained visual learning are documented in `assets/source/buildings/README.md`; no unverified generated asset is being loaded into the game.
- Added the first Service Host slice. A Host requires the visible Reception Shop Port, costs `$450` to hire and `$210` per week, improves arrival/shop conversion and is visibly stationed at the shop. The early workshop deliberately earns less with a Host than without one, so the role only becomes attractive as shop traffic and operational pressure grow. Autosave is schema v8.
- Replaced the temporary long management dock with venue menus: Overview, Programs, Team, Venue, Shop, Guests and Finance. Loans and opening-price controls are now deliberately hidden under Finance, while Overview keeps the venue pulse and Venue keeps construction. Browser QA confirmed Finance is absent from Overview and reachable only through its menu.
- Added the first deterministic guest-feedback content bank. Compact Gus, recovery, shop, queue, price and facility reactions now have contextual English variants instead of every visit repeating one sentence. The simulation selects only the family justified by that guest's actual route/outcome; a test protects visible variety in the first guest sample.
- Added Master courses: Heat Craft, Aroma Craft and Performance Craft now train independently from 0-10 with escalating in-game cash cost, no real-time wait gate, and direct compatibility with the existing Execution evaluation. Candidate recruitment remains the next staff expansion.
- Added the first individual Master equipment catalogue: Towel Set, Hand Fan, Infusion Kit and Rain Ladle. Each is a one-time purchase for the current Master and has a bounded, narrow delivery benefit; Heat/Aroma/Performance gating now recognises the matching Fan/Infusion/Towel tool. Autosave is schema v9.
- TODO: replace placeholder geometry with reviewed asset layers; turn guest samples into route-based live visits, add manual save/export/import and the complete content data model.

## 2026-08-25

- Fixed a maintenance data-loss bug identified in review: a facility under timed repair now remains unavailable without losing its stored condition or becoming implicitly healthy during a weekly settlement. `repairTask` remains the canonical under-maintenance marker. Added a regression test for a 12% Critical shower across a pending repair and week advance. Use-based wear and planned-versus-emergency repair economics remain part of the later numerical maintenance pass.

- Implemented the first shared Shop Fit slice: products now carry routine/recovery/social/premium/curious tags; Canal has a reusable market audience; the active Gus shifts that week's shop audience; assortment fit changes total conversion as well as item allocation; and Signature Towel is locked until the player saves a discovered Gus. Added Towel Rental as a distinct laundering-backed service line. Its later guest-equipment and visual towel-service behaviour remains a production task. Full tests (63) and build pass.

- Recorded the deferred Shop Fit package in `docs/later-review-list.md` and P2 overview: motivation/local-market-driven assortment demand, towel rental with the equipment/service animation loop, and branded merchandise gated by genuine venue/program identity. Do not implement these as isolated shop patches before the shared market and Brand Value systems exist.

- Fixed the prototype guest-feedback rotation bug: the old `week * 3 + index` selector cancelled the week term against three-line families, making a slot repeat its sentence forever. The temporary selector now rotates by week and is protected by a test. The approved persisted 30-visit sentence cooldown and 8-outcome root cooldown remain deferred to the full guest-memory implementation.

- Reviewed Claude's 2026-08-25 changes. Corrected one remaining negative-cash edge case: `Continue at risk` now requires an actually takeable loan offer, not merely a positive leftover credit figure. A state with $500 remaining credit but no offer small enough no longer permits an infinite grace loop. Added a dedicated state test. Targeted tests and production build pass; the standard headless smoke has no console failures, though its canvas screenshot remains black due to the known local WebGL capture limitation.

- Expanded Service Host into a three-person Service Team. Hire costs are $450, $900 and $1,500; weekly wages are $210, $260 and $330. Each member has a bounded, diminishing gain to reception capacity and shop conversion; pressure adds a small shared flow bonus rather than making the team a global multiplier. The scene renders one to three service placeholders by the shop. Existing one-Host saves load as a one-person team. Targeted simulation/game tests, production build and standard browser smoke all pass.

- Design adjustment: Service Host is not late-game-only. Once the visible Reception Shop exists, the Host is intended as a natural early operations hire: +8 normal reception places and 41% shop conversion; under arrival pressure this rises to +12 places and 44%. The low-volume scenario now verifies that the role can pay for itself, while the pressured scenario protects its stronger late value.

- Corrected the Service Host dead-end. The Host now adds a second reception/shop flow only once normal arrival demand reaches 90% of the venue's ordinary capacity: that grants 12 extra arrival places and 44% shop conversion under pressure, while the early low-volume Host remains a deliberate loss. The scenario suite now asserts both behaviours. The Program Sauna scenario was updated from an obsolete "never improves a week" assertion to a bounded-benefit check; it must not silently become a solved economy.

- Corrected the unreachable Large Secured Loan. The developed-venue credit ceiling is now $93k rather than $60k, so the $68.4k total repayment can be approved after all current Canal modules and 20 profitable weeks. The loan remains unavailable at a fresh venue. Automated coverage now proves Small, Standard and Large bands each have an available venue stage and that taking Large actually records the loan.

- Closed the free-opening-hours exploit. A venue day is capped at 16 hours; the UI permits 06:00-24:00 only; and weekly Master/Host staffing now scales with total public hours above the normal 50-hour week. Longer schedules can still be a strategic demand choice, but cannot create a free extra shift. Tests protect additional staffing cost and the 16-hour simulation cap.

- Replaced the fixed $24 admission-price cliff with an explicit value curve. Lower entry prices now purchase extra ordinary visits; visible facility upgrades, a Service Host and discovered Rare/Iconic/Ultimate programmes raise the price guests find credible. Prices far above that context fall off more sharply. Tests protect both low-price demand and the fact that a stronger venue sustains a higher price; the prior default ledger remains stable at $24.

- Closed a remaining review-score gap: Classic Ritual and Show Journey now each have Rare and Iconic Venue Fit paths, rather than being silently capped by intent. A complete Ultimate Classic Ritual can reach five stars in the currently modelled Canal facilities; an Iconic Show Journey can also do so with the Program Sauna and Outdoor Gus Yard. New evaluator tests protect both paths, including an $18 supplement so premium delivery is not treated as automatically inferior.

- Completed the Claude-review P0 corrections that are useful at the prototype stage. Program Execution can now reach `Rare` and `Iconic` when composition, Master craft/style and price support it; Venue Fit has explicit bounded `Iconic` paths; Canal has real shower-to-plunge and bridge-to-water anchors/routes; and unreadable autosaves are preserved rather than silently replaced with a new game.
- Replaced the Canal placeholder renderer's separate `640 x 900` coordinate table with the canonical `960 x 640` scene contract. The portrait app now uses a `456 x 640` camera viewport into that same world, including world-coordinate module selection and horizontal drag panning. This makes future buildings, paths, guests and effects use one authored coordinate system.
- Deferred by decision: real-time week progression, offline catch-up and full save migrations. `Run Week` remains an intentional prototype/debug tool; later work must not present it as final time design.
- Verification after the scene refactor: production build passed; all 52 Vitest tests passed; the required Playwright smoke test ran with no errors; visual inspection of `output/web-game-2/shot-1.png` confirmed the portrait camera, shared world geometry and canal presentation. Existing bundle-size warning remains non-blocking.

- Added a deterministic first Canal balance simulator: Master hire, six reference modules, admission pricing, weekly revenue/costs and a player-readable operating signal.
- Browser-tested owner-only week, Master hire, weekly result and entrance-sign purchase. The first sign contribution was corrected from +80 to the documented +60 per week.
- Added independent real-time construction projects with paid rush completion, three chain loan products, weekly repayment, borrowing room and a negative-cash decision card. Added state tests for construction completion and loan repayment.
- Rebased the first venue from abstract units to rounded US dollars: $24 entry, $1.2k sign, $3k shower, $9.5k plunge, $16k outdoor Gus field and $32k workshop programme expansion. Loans were lengthened so an early loan is pressuring but serviceable.
- Rewrote the temporary Finance panel in plain English: cash received now, repayment per game week, term, total repayment and an explanation of credit capacity.
- Next: move loan/shop/staff controls out of the temporary test dock into the later management navigation; widen scenario coverage for recovery and programme capacity, then replace the weekly calculator inputs with real guest/queue event resolution.
- Added first transparent Master recruitment: a Starter Master is the initial candidate, and a Player can request a free 60-minute, `$125` 20-minute, or `$350` immediate shortlist. The fee changes waiting time only, never candidate quality. Each candidate exposes the three craft ratings, style, hiring fee and real weekly wage; exactly one Master can be active at Canal. A Master may be dismissed. The selected Master wage now flows into the weekly operating ledger instead of every candidate silently costing the same amount. Autosave is schema v10 and accepts the earlier v9 local autosave shape with defaults.
- Added automated coverage for timed Master search and candidate wage economics. Browser QA exercised the candidate menu and an immediate shortlist on the mobile layout, with no browser errors. Current test suite: 40 tests passed; production build passed (existing non-blocking bundle-size warning remains).
- Connected maintenance condition to physical operations. Program Sauna condition reduces its real Special Gus seats; Shower and Cold Plunge condition reduce recovery throughput and can produce a justified cold-recovery queue. The Team panel now explains each facility's percentage, state and specific reduced function before quoting a repair. Automated tests cover degraded Program Sauna capacity and degraded-shower queue pressure. A browser scenario built a shower, ran 31 weeks, verified `69% · Worn` and `Recovery flow reduced 5%` without errors. Current test suite: 42 tests passed; production build passed.
- Added a visible world-state bridge for maintenance and construction. Worn/degraded technical fields now get a small amber status badge in the Canal scene; fields under construction emit a restrained animated steam plume plus `BUILD` label while the Venue menu owns the real-time countdown and optional normal-currency rush. Browser QA found and fixed a countdown off-by-one display, so a three-hour Reception Shop project now correctly reads `3 h`, not `4 h`.
- Added the first reusable `Saved Gus` repertoire. A composition can be saved only after first delivery reveals its tier, retains the player-written name and ordered recipe, cannot be duplicated merely by renaming it, and loads back as the active Gus with Composition preserved. Execution, Venue Fit and stars remain live outcomes. Autosave/export is schema v11 and now persists the repertoire. Browser QA hired the first Master, ran Canal Ritual, saved it and confirmed the visible card; suite is now 43 tests plus a passing production build.
- Made the weekly ledger player-readable without changing Canal's existing economic calculation: reports now separately show entry/Gus/shop income; venue, staff, operations, inputs and loan costs; plus requested versus feasible Gus sessions. Browser QA confirmed the healthy first week renders `$1,680` entry, `$98` Gus, the cost categories and `2/2` viable sessions with no errors. Updated `first-slice-balance-audit-v0.1.md` to distinguish the original pre-connection audit from the current completed P0 bridges.
- Connected opening windows to programme fit without demographic stereotypes. The simulation now preserves Canal's neutral 10:00-20:00 reference while rewarding a clearly later social/show window and daytime recovery fit only where it makes operational sense. Finance explains the current `Natural`, `Mixed` or `Awkward` fit before the player runs the week; reports preserve the same explanation. A scenario test protects this, and browser QA verified a 16:00-24:00 Social Energy plan renders its natural-fit explanation. Suite: 44 tests passed; production build passed.
- Replaced the aggregate Shop placeholder with a player-managed three-item assortment and genuine item economics. Cold Water, Herbal Tea, Sauna Towel, House Blend Oil, Fruit & Nut Pack and higher-cost venue-logo Signature Towel have visible sale/procurement prices and purpose copy. The weekly shop result allocates every purchase to selected line items, feeding the same transparent ledger. Autosave/export is schema v12 and migrates earlier local schemas with the default assortment. Browser QA built and rushed Reception Shop Port, then replaced House Blend Oil with Signature Towel without errors. Suite: 47 tests passed; production build passed.
- Connected visible guest motivation to actual routing. Each sampled guest now has a purpose-derived Gus fit: compatible visitors use the planned program, a representative open guest keeps programme activity visible, while others take a basic sauna instead of being falsely shown enjoying every Gus. Shop stops now require a shop-friendly visit purpose. The inspection card exposes only natural-language `Program fit`, never a score or demographic assumption. Browser QA verified the updated guest card; suite remains 47 tests and build passes.
- Upgraded the temporary guest scene motion from a single jump to playback of each guest's completed route so far. The renderer follows the same saved `visitPath` and `currentStop` used by the guest profile, then remains at the truthful current activity. Direct mobile browser QA confirmed the first week has routed Gus guests at the workshop entry and exiting guests on their departure path, with the saved state reporting the matching `arrival → program → exit` routes.
- Made requested Gus frequency a real operating control. Each viable requested session now creates its own potential programme demand, while material inputs scale with feasible sessions and room/Master constraints still cap delivery. Direct mobile QA ran four requested sessions and showed `28/32` Gus guests, `$16` inputs and `4/4` viable sessions; suite: 48 tests and production build passed.
- Connected a first bounded Venue Fit benefit to weekly demand and price tolerance. Recovery-supported Quiet Recovery, Social Energy in Outdoor Gus Yard, Show Journey in a dedicated programme space and Classic Ritual in Program Sauna can gain a small physical-fit demand benefit, stated plainly in the ledger. It does not generate capacity or make an expansion automatically profitable. Mobile QA built the yard through an ordinary loan, ran Social Energy and correctly showed `22/40` Gus guests plus the physical-fit explanation and the `$750` loan pressure. Updated the stale balance audit to reflect all implemented P0 bridges. Suite: 49 tests and production build passed.
- Reconciled the Canal numeric reference with the running model: the healthy ledger now lists the actual `$400` venue, `$500` Master, `$312` operations and `$8` two-session input cost; the default schedule is five days/50 hours; Bench Refit is included as the documented sole low-visibility capacity exception; and the financed Outdoor Gus Yard example no longer claims a fictitious immediate profit. `remaining-work-overview-v0.1.md` now marks the connected Canal P0 loop complete and makes the protected numerical audit the next P1 deliverable.
- Added `src/sim/canalScenarioMatrix.ts` as the one runnable eight-case Canal balance contract, with an automated test for its core relationships: owner routine, healthy starter, unsupported entry price, higher frequency, capacity before demand, fitting social yard, cold recovery pressure and loan pressure. The Canal reference now links directly to that contract. Suite: 51 tests across 10 files and production build pass; the standard Playwright loop also completed without errors (its canvas-only screenshot remains blank because of the local headless WebGL capture limitation, while direct full-page mobile screenshots were used for UI QA).
- Began the approved Canal visual-production phase without promoting unverified art into runtime. Generated `assets/source/backgrounds/canal-01-quay-parcel-draft-v02.png` as a source-only waterfront parcel reference: its central unoccupied building footprint, fixed quay, water edge and independent pads are stronger composition evidence than the older draft, but it remains too detailed and geometrically mismatched for the 960 x 640 runtime contract. Generated `assets/source/buildings/repair-workshop-base-draft-v02.png` as a source-only workshop silhouette; verified it is a real RGBA PNG with alpha, unlike rejected v01. Added empty-but-explicit Canal production, sprite and effect packages with every current runtime layer, module field, anchor and first animation responsibility recorded before asset slicing begins.
- Added the approved overview-scene upgrade inspection bridge. A tap on a built Canvas module now selects only a real built upgrade, outlines its fixed field and shows its English name/effect plus `View in Venue`; the selector state is present in no-login save data and debugging text. Corrected a related mobile rendering/input bug by preserving the Phaser playfield's `640 x 900` aspect ratio instead of CSS-stretching it vertically. Direct browser QA built/rushed the Entrance Sign, tapped it in the scene and verified the selected `arrival` state/card with no browser errors. Suite remains 52 tests and production build passed before the final direct interaction check; standard Playwright smoke completed without errors.
- Added the first Canal shared-effect bridge in the placeholder renderer: gentle continuous canal ripples and chimney steam, plus event-driven outdoor Gus steam, shower water and cold-plunge ripple loops. Effects are triggered only by the appropriate built module and sampled guest activity, matching the later `assets/effects/canal` contract. Mobile QA financed/build-rushed shower plus cold plunge, selected a real Cold Plunge finish, ran the week and visually confirmed the active recovery scene with no browser errors. Suite remains 52 tests and production build passed; standard smoke completed without errors.
- Added `assets/source/sprites/guest-walk-reference-draft-v01.png`, a verified-alpha four-direction/four-phase walk source reference. It is intentionally recorded as a movement reference only: it gives the later sprite pass a readable gait, but is too large, detailed and chibi-like for the approved compact Pokémon-adjacent runtime scale. `assets/source/sprites/README.md` now defines the required neutral modular body/appearance/action pipeline before any generated sheet can be promoted.

- Corrected Gus discovery persistence. Renaming a Gus or changing its requested sessions or supplement price now keeps the discovered Composition Tier; `updateProgram` compares the canonical recipe signature before resetting it, so only an actual composition change makes a revision unproven. Targeted game tests (20) and the production build pass.

- Made Cold recovery queue pressure economically real. Canal now resolves its recovery capacity before finalising the ledger: visits lost to an overfull/degraded cold route reduce admitted visits, Gus revenue, shop conversion and net result. The guest sample reads that same canonical result, so its queue state cannot disagree with the ledger. A healthy shower clears the tested one-session Cold Plunge bottleneck and improves the weekly result. Targeted balance, guest and game tests (39) plus production build pass. A complete per-event visitor simulation remains the later P1 replacement for this deliberately bounded aggregate channel model.

- Replaced calendar-based condition decay with use-based wear. Program Sauna wear follows actual Gus attendance; Shower and Cold Plunge wear follow actual recovery demand. The same recovery channel controls degraded capacity, lost visits and the repair decision, with no separate revenue coefficient. An unused facility does not decay, while an active Cold Plunge recovery route now loses four condition points per busy weekly run and reaches the first visible wear band within a normal multi-day test. Save data preserves the recovery-demand value in the latest report. Targeted tests (40) and production build pass.

- Removed the stale competing Canal capacity envelope from the balance audit. `src/content/canalCapacity.ts` is now the only authored source for compact-room, Bench Refit, Outdoor Gus Yard and Program Sauna Special Gus capacity; the simulator imports it and the design documents refer to it rather than duplicating numeric values.

- Clarified the real-time/offline decision after review item T1. The current `Run Week` implementation remains a deliberate prototype tool, and `simulation-contract-v0.1.md` already labels the complete time model as not built. `remaining-work-overview-v0.1.md` now makes deterministic `advanceSimulation`, persisted `lastSimulatedAt`, named RNG streams and online/offline equivalence a release gate before any multi-day external test or Render deployment, rather than implying this can wait behind later content work.

- Reviewed the save-migration warning. The claimed silent overwrite is already prevented: `loadAutosave()` preserves unreadable/pre-v9 saves by throwing, and App deliberately never enables autosave after that failure. Full migration functions, autosave history and raw recovery export remain necessary before external multi-day testing and are now explicit items in the real-time release gate.

- Removed the misleading Dexie no-op version ladder. IndexedDB remains at its deployed store-layout version (`6`) to avoid downgrading existing local databases; an explicit code comment distinguishes that storage/index version from `SAVE_SCHEMA_VERSION`, which belongs to serialized game-state migrations.

- Removed UI selection from save/export/import data. Guest and map-module selection remain temporary presentation state and now disappear on reload as intended. The latest report remains temporarily persisted as the completed historical week event, not a recomputable view; the real-time release gate now requires replacing it with a compact versioned return-event summary when the canonical simulation is built.

- Added best-effort local-save flushing for mobile lifecycle events. The queued autosave now immediately writes current state when the page hides or document visibility becomes hidden, covering a backgrounded iOS tab before the normal debounce completes. Offline catch-up on return remains deliberately deferred to the `advanceSimulation` release-gate work; the temporary prototype must not invent elapsed weeks on visibility change.

- Replaced the Canal flat module lists with `src/content/venueModules.ts`. Each current module now has a typed data card covering owner layer, compatibility, capacity, routes, economy, condition and visual field/package. Canal Workshop derives its exact buildable catalogue through `location × building` tag matching; the save schema, balance types, game store and scene module fields derive their IDs from that source. Content tests validate the complete cards, scene fields and route anchors before build.

- Moved Phaser behind a dynamic scene import. The React management shell now paints while the venue scene downloads and displays a small loading state in its frame. Added a GitHub Actions verification workflow and a `check:bundle` budget that fails if the management entry exceeds 450 kB uncompressed or the separate Phaser scene chunk disappears.

- Replaced all `latest` dependency specifiers with tested caret ranges and set the Node floor required by the current native build tooling. CI now runs the frozen lockfile on both Linux and macOS. The technical blueprint records the deliberately selected Phaser 4, React 19, Vite 8, Vitest 4, TypeScript 7 and Zod 4 major baseline.

- Added `tsconfig.test.json` to the TypeScript build graph, so `tsc -b` typechecks `tests/` as well as runtime code. Replaced the incomplete Master fixture with a typed `makeMaster()` factory and added `makeProgram()`/`makeVenue()` shared defaults to prevent hand-built test objects drifting from domain types.

- Replaced CanalScene's full redraw-on-notify renderer with persistent layers. Terrain/workshop and ambient water/chimney effects are created once; module graphics, state effects, maintenance markers, service staff and guests update only when their own state keys change. Guest containers are pooled by guest id, so taps, venue-name edits and unrelated menu actions no longer recreate or restart their route tweens. Full typecheck, 69 tests, production build and bundle budget pass. Playwright smoke advanced to Week 2 with no console errors; visual inspection of `output/web-game/shot-0.png` confirmed the persistent scene renders correctly.

- Consolidated Canal input into Phaser. Built-module hit zones are now authored directly from scene fields and guest containers use the same Phaser event system; the competing DOM pointer listener and duplicate coordinate conversion are removed. A tap on empty scene clears module selection only when Phaser reports no interactive object; a guest explicitly stops propagation so it cannot also reselect a module beneath it. Empty build fields also clear the current selection. Full typecheck and 69 tests pass; the local mobile smoke advances a week without console errors.

- Fixed tablet canvas distortion. The game frame now keeps the provisional `456 x 640` scene aspect at every breakpoint and CSS no longer forces Phaser's canvas to fill both axes, so Phaser `FIT` controls letterboxing rather than stretching pixels. Whole-number scene scaling remains a final-art/render QA decision: enforcing it now would clip or waste much of a responsive phone viewport whenever the available size is below the fixed scene. Production build passes.

- Activated iPhone safe-area handling. `viewport-fit=cover` enables the browser insets, while the shared body layout applies all four safe-area edges before either the Phaser scene or management dock is laid out. The dock therefore no longer needs a special bottom-only inset, and landscape cutouts are covered too. Production build passes; final device QA must include a notched iPhone because desktop browsers resolve the insets to zero.

- Established mobile readability floors for the management dock. Shared CSS tokens now set `11 px` as the compact text floor, `44 px` as the interactive target floor and darker muted/disabled text colours. Navigation, inputs, selects, panel buttons, ledger breakdown and guest/loan metadata inherit these rules instead of introducing new micro sizes. The dock is intentionally taller; final visual QA must assess the resulting scene-to-controls balance on an actual phone. Full suite: 69 tests passed; production build passes.

- Removed development QA hooks from production. `render_game_to_text` and `advanceTime` now live in `src/dev/qaHooks.ts` and are dynamically imported only in Vite development mode; the production asset scan confirms neither global name nor the module is shipped. The local automated smoke path still completes, while public builds cannot use console hooks to read state or advance weeks. Full suite: 69 tests passed; production build passes.

- Removed unused Zustand and documented the intentional small hand-rolled domain store in the technical blueprint. Playwright is now exercised by `tests/e2e/canal-flow.mjs`: a real mobile browser starts a clean game, changes a Gus, runs a week, builds/rushes the Entrance Sign, reloads and confirms the autosaved week/program/amenity persist. CI installs Chromium and runs that flow on Linux and macOS. Frozen install, 69 Vitest tests, E2E flow, production build and the management bundle budget pass.

- Removed `this` binding from the game-store command path. Construction resolution, Master hiring, repair pricing/resolution and weekly settlement are named module-level functions over the private state; `gameStore` exposes them as a flat facade. A regression test destructures construction, Master and weekly commands before invoking them, proving normal callback/destructuring use cannot lose an object receiver. Full suite: 70 tests passed; production build passes.

- Corrected the document language to English, matching the locked player-copy language and allowing screen readers to pronounce venue, guest and Gus text correctly.
